name: Open Pull Request

on:
  workflow_call:
    inputs:
      issue_number:
        required: true
        type: string
      issue_title:
        required: true
        type: string
      branch_name:
        required: true
        type: string
      patch_artifact_name:
        required: true
        type: string
      summary_artifact_name:
        default: ""
        type: string
      pr_title:
        required: true
        type: string
      commit_message:
        required: true
        type: string
      labels:
        default: ""
        type: string
      render_status_message:
        default: ""
        type: string

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  open_pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Download patch
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.patch_artifact_name }}
          path: /tmp/pr-patch

      - name: Apply patch
        env:
          PATCH_PATH: /tmp/pr-patch/${{ inputs.patch_artifact_name }}
        run: git apply --index "$PATCH_PATH"

      - name: Download summaries
        if: inputs.summary_artifact_name != ''
        uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.summary_artifact_name }}
          path: /tmp/pr-summaries

      - name: Build pull request body
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          PR_BODY_PATH: /tmp/pr_body.md
          INCLUDE_SUMMARIES: ${{ inputs.summary_artifact_name != '' }}
          RENDER_STATUS_MESSAGE: ${{ inputs.render_status_message }}
          GENERATOR_SUMMARY_PATH: /tmp/pr-summaries/generator_summary.md
          REVISION_SUMMARY_PATH: /tmp/pr-summaries/revision_summary.md
        run: bash .github/scripts/build_pr_body.sh

      - name: Create pull request
        uses: peter-evans/create-pull-request@v6
        with:
          branch: ${{ inputs.branch_name }}
          title: ${{ inputs.pr_title }}
          commit-message: ${{ inputs.commit_message }}
          body-path: /tmp/pr_body.md
          labels: ${{ inputs.labels }}
